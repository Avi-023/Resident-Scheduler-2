# Core dependencies
streamlit>=1.31,<2.0
pandas>=2.1,<3.0
numpy>=1.26,<3.0
altair>=5.0,<6.0

# Optional but recommended for better performance
pyarrow>=14,<20

# Excel export support
xlsxwriter>=3.1,<4.0
openpyxl>=3.1,<4.0

# Optional optimizer (schedule can be generated without it)
# pulp>=2.7,<3.0
